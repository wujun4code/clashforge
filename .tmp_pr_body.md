## Summary
- detect ClashForge firewall takeover for both nftables and iptables backends
- stop requiring `inet metaclash` table presence to mark firewall module as managed
- rename module title from `NFT / 防火墙` to `防火墙规则` to avoid backend bias

## Why
Some routers run iptables backend, so nft table check is false and UI misses the firewall takeover status.

## Test
- go test ./cmd/clashforge
- go test ./internal/api *(currently has unrelated pre-existing test compile issue: undefined: chi.NewContext)*